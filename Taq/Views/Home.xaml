<Page
    x:Class="Taq.Views.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Taq.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:TaqShared.Models"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled">
    <Page.Resources>
        <Style x:Key="txtBlkSty1" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="TextAlignment" Value="Left" />
            <Setter Property="Margin" Value="0, 0, 10, 0" />
            <Setter Property="MinWidth" Value="80" />
        </Style>
        <DataTemplate x:Key="SiteListHeaderTemplate">
        </DataTemplate>
        <DataTemplate x:Key="SiteListDataTemplate" x:DataType="data:Site">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{x:Bind siteName}" Style="{StaticResource txtBlkSty1}"/>
                <TextBlock Text="{x:Bind County}" Style="{StaticResource txtBlkSty1}" />
                <TextBlock Text="{Binding Pm2_5}" Style="{StaticResource txtBlkSty1}" />
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Horizontal" Margin="22, 10, 0, 0">
            <TextBlock Text="觀測站" Style="{StaticResource txtBlkSty1}" FontWeight="Bold"/>
            <TextBlock Text="縣市" Style="{StaticResource txtBlkSty1}" FontWeight="Bold"/>
            <TextBlock Text="PM 2.5" Style="{StaticResource txtBlkSty1}" FontWeight="Bold" />
        </StackPanel>
        <ListView
                ItemsSource="{x:Bind app.sites}"
                x:Name="listView"
                ItemTemplate="{StaticResource SiteListDataTemplate}"
                SelectionMode="Single"
                IsItemClickEnabled="True"
                ItemClick="listView_ItemClick"
                Margin="10,30,10,79"/>

        <Button x:Name="button"  FontFamily="Segoe MDL2 Assets" Content="&#xE895;"  HorizontalAlignment="Left" Margin="10,0,0,10" VerticalAlignment="Bottom" Click="button_Click" Height="32" ToolTipService.ToolTip="更新" Width="35"/>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="50,0,0,10" TextWrapping="Wrap" Text="狀態：" FontSize="20" Height="32" VerticalAlignment="Bottom"/>
        <TextBlock x:Name="statusTextBlock" HorizontalAlignment="Left" Margin="110,0,0,10" TextWrapping="Wrap" Text="訊息" FontSize="20" Height="32" VerticalAlignment="Bottom"/>
        <TextBlock x:Name="textBlock_Copy" HorizontalAlignment="Left" Margin="10,0,0,47" TextWrapping="Wrap" Text="任選一觀測站作訂閱" FontSize="20" Height="27" VerticalAlignment="Bottom"/>
    </Grid>
</Page>
